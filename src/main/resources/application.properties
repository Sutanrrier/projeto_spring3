# Configuracao Banco de dados
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/projeto_spring3
spring.datasource.username=root
spring.datasource.password=mysql

spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# MULTIPART
spring.servlet.multipart.enabled=true
spring.servlet.multipart.file-size-threshold=2KB
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=25MB

# PostgreSQL Only
# spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Flyway Configuration
spring.flyway.locations=classpath:db/migration

# Swagger Configuration
springdoc.pathsToMatch=/api/**/v1/**,/auth/**
springdoc.swagger-ui.use-root-path=true

# CORS
cors.originPatterns=http://localhost:3000,http://localhost:8080,https://sutanrrier.com.br

#File Upload
file.upload-dir: /Users/Pablo/Documents/Workspaces/resources/UploadDir

#Spring Security
security.jwt.token.secret-key=CH8JbzB%RwzTn8L
security.jwt.token.expire-length=3600000